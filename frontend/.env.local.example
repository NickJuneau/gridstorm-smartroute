# Backend API base URL for /api/analyze proxy
BACKEND_URL=http://localhost:8000

# Backend API base URL for /api/simulate proxy (optional)
BACKEND_URL_SIM=http://localhost:8000

# Backend analyze-file endpoint override (optional)
BACKEND_URL_FILE=http://localhost:8000/analyze-file

# Optional deterministic mock mode for demos without backend
# FALLBACK_MOCK=1
